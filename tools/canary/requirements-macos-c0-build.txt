# Nuitka publishes a source archive, not a wheel. Build isolation is disabled in
# the workflow so no unpinned build dependency can be fetched behind this hash.
Nuitka==4.1.2 \
    --hash=sha256:efc2359b171d7b63046ca8ec8dee57015c3466a9df74b68a049c2c1a7e93ecee
